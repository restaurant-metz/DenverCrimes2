declare class Daemon {
    private port;
    private host;
    private ovpnFile;
    private wait;
    private up;
    private down;
    private sudoPasswd;
    constructor({ sudoPasswd, ovpnFile, port, host, wait, up, down }: {
        sudoPasswd?: string;
        ovpnFile?: string;
        port?: number;
        host?: string;
        wait?: number;
        up?: string;
        down?: string;
    });
    private checkProcessList;
    start: () => Promise<void>;
    isRunning: () => Promise<boolean>;
    kill: () => Promise<void>;
}
export default Daemon;
